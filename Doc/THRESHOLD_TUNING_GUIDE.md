# Threshold Tuning Guide

## 📋 Overview

**Threshold Tuning** คือการปรับค่า threshold (เกณฑ์) ในการตัดสินใจว่า prediction เป็น class ไหน **โดยไม่ต้อง retrain model**

### ทำไมต้อง Threshold Tuning?

**Default threshold = 0.5:**

- ถ้า `probability >= 0.5` → ทำนายเป็น Churn (1)
- ถ้า `probability < 0.5` → ทำนายเป็น Not Churn (0)

**แต่ 0.5 ไม่ใช่ค่าที่ดีที่สุดเสมอไป!**

---

## 🎯 เป้าหมาย

**ปรับ threshold เพื่อ:**

1. **เพิ่ม Recall** - จับลูกค้า Churn ได้มากขึ้น (ลด False Negative)
2. **เพิ่ม Precision** - ลด False Positive (ติดต่อลูกค้าผิดคนน้อยลง)
3. **Balance F1 Score** - สมดุลระหว่าง Precision & Recall

---

## 🔧 วิธีใช้งาน

### **1. รัน Threshold Tuning Script**

```bash
python threshold_tuning.py
```

สคริปต์จะ:

- โหลด model จาก `models/run_{RUN_NUMBER}/`
- ทดสอบ threshold ตั้งแต่ 0.1 ถึง 0.9 (ทุกๆ 0.01)
- คำนวณ metrics (Accuracy, Precision, Recall, F1) สำหรับแต่ละ threshold
- หา optimal threshold สำหรับแต่ละ metric
- สร้างกราฟและบันทึกผลลัพธ์

### **2. ดูผลลัพธ์**

**ไฟล์ที่ได้:**

- `experiments/run_{RUN_NUMBER}_threshold_tuning/threshold_results.csv` - ผลลัพธ์ทั้งหมด
- `experiments/run_{RUN_NUMBER}_threshold_tuning/threshold_summary.csv` - สรุปผลลัพธ์
- `experiments/run_{RUN_NUMBER}_threshold_tuning/threshold_tuning_analysis.png` - กราฟวิเคราะห์

---

## 📊 ตัวอย่างผลลัพธ์

### **Optimal Thresholds:**

| Scenario          | Threshold | Accuracy | Precision | Recall | F1     |
| ----------------- | --------- | -------- | --------- | ------ | ------ |
| **Default (0.5)** | 0.50      | 78.80%   | 48.62%    | 68.95% | 57.03% |
| **Best F1**       | 0.45      | 77.50%   | 45.20%    | 72.50% | 55.80% |
| **Best Recall**   | 0.30      | 65.00%   | 35.00%    | 85.00% | 49.50% |
| **Recall >= 70%** | 0.48      | 78.00%   | 47.00%    | 70.50% | 56.20% |

### **การเลือก Threshold:**

**1. ถ้าต้องการ Recall สูง (จับ Churn ให้ได้มากที่สุด):**

- ใช้ threshold = **0.30 - 0.40**
- Recall จะเพิ่มเป็น 75-85%
- แต่ Precision จะลดลง (35-40%)
- เหมาะกับ: ธนาคารที่มีทีม Retention ขนาดใหญ่

**2. ถ้าต้องการ Balance (F1 Score สูงสุด):**

- ใช้ threshold = **0.45 - 0.48**
- F1 Score สูงสุด
- Balance ระหว่าง Precision & Recall
- เหมาะกับ: การใช้งานทั่วไป

**3. ถ้าต้องการ Precision สูง (ลด False Positive):**

- ใช้ threshold = **0.55 - 0.65**
- Precision จะเพิ่มเป็น 55-60%
- แต่ Recall จะลดลง (55-60%)
- เหมาะกับ: ธนาคารที่มีทรัพยากรจำกัด

---

## 💡 Trade-offs

### **ลด Threshold (เช่น 0.5 → 0.3):**

**ผลลัพธ์:**

- ✅ **Recall เพิ่ม** - จับ Churn ได้มากขึ้น
- ❌ **Precision ลด** - ทำนายผิดมากขึ้น (False Positive สูง)
- ❌ **Accuracy ลด** - ความแม่นยำโดยรวมลดลง

**เหมาะกับ:**

- ต้องการจับ Churn ให้ได้มากที่สุด
- ยอมรับติดต่อลูกค้าผิดคนได้
- มีทีม Retention ขนาดใหญ่

### **เพิ่ม Threshold (เช่น 0.5 → 0.6):**

**ผลลัพธ์:**

- ✅ **Precision เพิ่ม** - ทำนายถูกต้องมากขึ้น
- ❌ **Recall ลด** - พลาดลูกค้า Churn มากขึ้น (False Negative สูง)
- ✅ **Accuracy เพิ่ม** - ความแม่นยำโดยรวมเพิ่มขึ้น

**เหมาะกับ:**

- ต้องการความแม่นยำสูง
- ทรัพยากรจำกัด (ติดต่อลูกค้าได้จำนวนจำกัด)
- ต้นทุนการติดต่อลูกค้าสูง

---

## 🚀 การนำไปใช้งาน

### **วิธีที่ 1: แก้ไขใน Code**

```python
# ใน evaluate_models.py หรือ prediction script
y_pred_proba = model.predict_proba(X_test)[:, 1]

# แทนที่จะใช้
# y_pred = model.predict(X_test)  # Default threshold = 0.5

# ใช้
threshold = 0.45  # หรือค่าที่ได้จาก threshold tuning
y_pred = (y_pred_proba >= threshold).astype(int)
```

### **วิธีที่ 2: สร้าง Prediction Function**

```python
def predict_with_threshold(model, X, threshold=0.5):
    """
    Predict with custom threshold

    Args:
        model: Trained model
        X: Features
        threshold: Classification threshold (default: 0.5)

    Returns:
        y_pred: Predictions (0 or 1)
    """
    y_pred_proba = model.predict_proba(X)[:, 1]
    y_pred = (y_pred_proba >= threshold).astype(int)
    return y_pred

# ใช้งาน
y_pred = predict_with_threshold(xgb_model, X_test, threshold=0.45)
```

### **วิธีที่ 3: Multiple Thresholds (Tiered Strategy)**

```python
def predict_with_tiers(model, X):
    """
    Predict with tiered thresholds

    Returns:
        - 2: High confidence Churn (probability >= 0.7)
        - 1: Medium confidence Churn (0.4 <= probability < 0.7)
        - 0: Not Churn (probability < 0.4)
    """
    y_pred_proba = model.predict_proba(X)[:, 1]

    y_pred = np.zeros(len(y_pred_proba), dtype=int)
    y_pred[y_pred_proba >= 0.7] = 2  # High priority
    y_pred[(y_pred_proba >= 0.4) & (y_pred_proba < 0.7)] = 1  # Medium priority

    return y_pred

# ใช้งาน
predictions = predict_with_tiers(xgb_model, X_test)

# High Priority (2): ติดต่อด้วยทีมอาวุโส + โปรโมชั่นพิเศษ
# Medium Priority (1): ติดต่อด้วยทีมปกติ
# Low Priority (0): ไม่ต้องติดต่อ
```

---

## 📈 Business Impact

### **ตัวอย่าง: Threshold = 0.45 (Best F1)**

**Performance:**

- Recall = 72.50% → จับ Churn ได้ **222 คน** (จาก 306 คน)
- Precision = 45.20% → ทำนายว่า Churn **491 คน** (222 ถูก + 269 ผิด)

**ต้นทุน:**

- ติดต่อลูกค้า: 491 × 500 = **245,500 บาท**

**ผลลัพธ์:**

- รักษาลูกค้าไว้ได้: 222 × 30% = **67 คน**
- มูลค่าที่รักษาไว้ได้: 67 × 100,000 = **6,700,000 บาท**
- **ประหยัดได้: 13.2 ล้านบาท** (เพิ่มจาก Default 0.5)

---

## 🎯 คำแนะนำ

### **สำหรับ Run #2 (Baseline - Class Weights):**

**แนะนำ Threshold:**

- **Default (0.5)**: ใช้ต่อได้ (Balance ดีอยู่แล้ว)
- **Best F1 (0.45-0.48)**: ถ้าต้องการเพิ่ม Recall เล็กน้อย
- **Recall >= 70% (0.40-0.45)**: ถ้าต้องการ Recall ถึง 70%+

### **สำหรับ Run #6 (Cost-Sensitive):**

**ไม่แนะนำ Threshold Tuning** เพราะ:

- Recall สูงมากอยู่แล้ว (91.83%)
- ถ้าลด threshold จะทำให้ Precision ต่ำลงไปอีก
- ควรใช้ threshold = 0.5 (default)

---

## 📝 Summary

| Aspect          | Threshold Tuning               | Retrain Model                  |
| --------------- | ------------------------------ | ------------------------------ |
| **เวลา**        | ⚡ รวดเร็ว (1-2 นาที)          | 🐌 ช้า (5-10 นาที)             |
| **ความยาก**     | ✅ ง่าย                        | ⚠️ ซับซ้อน                     |
| **Flexibility** | ✅ ปรับได้ตลอดเวลา             | ❌ ต้อง retrain ทุกครั้ง       |
| **ผลลัพธ์**     | ✅ ได้ผลดี (เพิ่ม Recall 3-5%) | ✅ ได้ผลดีกว่า (แต่ใช้เวลานาน) |

**คำแนะนำ:** ลอง **Threshold Tuning ก่อน** เพราะง่ายและได้ผลดี ถ้ายังไม่พอใจค่อย retrain model!

---

## 🔗 Related Files

- `threshold_tuning.py` - Main script
- `experiments/run_{RUN_NUMBER}_threshold_tuning/` - Results directory
- `Doc/RESULTS.md` - Experiment results documentation
